# `comms`

Продуктовый чат Space. Status: live / kernel. C1 закрыт 2026-08-28.

HTTP: channels + messages. WS: `/api/spaces/{spaceId}/channels/{channelId}/ws`.
Inbound: `internal.comms.inbound` / `events.integ.message.inbound`.

Подробно: [TODO.md](../TODO.md).
