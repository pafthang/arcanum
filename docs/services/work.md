# `work`

Issue-агрегат. Status: live / kernel.

HTTP: `GET/POST /api/spaces/{spaceId}/issues`, `GET/PATCH .../issues/{issueId}`, comments, labels, `GET .../work/overview`.
Events: `events.work.issue.{created,updated,assigned}`.

Нет: priority, due, sub-issues, relations, cycles, projects.

Подробно: [TODO.md](../TODO.md), [GAPS.md](../GAPS.md).
